services:
  parity-tests:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: workflow-mail-parity-tests
    command:
      - mvn
      - -Dmaven.repo.local=/tmp/.m2
      - -Dparity.enabled=true
      - -Dparity.java.base-url=${PARITY_JAVA_BASE_URL:-http://workflow-mail-java:9018}
      - -Dparity.go.base-url=${PARITY_GO_BASE_URL:-http://workflow-mail-go:8080}
      - test
