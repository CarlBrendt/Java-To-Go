services:
  parity-tests:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: workflow-scheduler-parity-tests
    command:
      - mvn
      - -Dmaven.repo.local=/tmp/.m2
      - -Dparity.enabled=true
      - -Dparity.java.base-url=${PARITY_JAVA_BASE_URL:-http://workflow-scheduler-java:9016}
      - -Dparity.go.base-url=${PARITY_GO_BASE_URL:-http://workflow-scheduler-go:8080}
      - test
