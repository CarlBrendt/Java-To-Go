┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 1. Сканирование Java-кода (MCP tool: analyze_java_service)│
│ - парсинг контроллеров Spring (@RestController, @RequestMapping)│
│ - извлечение DTO/моделей                                        │
│ - построение графа вызовов (контроллер → сервис → репозиторий)  │
│ - выявление внешних зависимостей (HTTP clients, DB, Kafka и пр.)│
│ Артефакт: api_contract.json + dependency_graph.json            │
│ Роль AI: генерация описаний, группировка эндпоинтов             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 2. Контрольная точка №1: утверждение API-контракта         │
│ - инженер проверяет и правит извлеченные маршруты/структуры     │
│ - фиксация контракта как эталона для тестов                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 3. Генерация черновика Go-сервиса (MCP tool: generate_go) │
│ - scaffolding: main.go, router, middleware (logging, recovery)  │
│ - генерация типов данных (Java class → Go struct + json tags)   │
│ - заглушки хендлеров с вызовом "business logic placeholder"     │
│ - миграция простых маппингов (без side effects)                 │
│ Артефакт: internal/handlers/*, internal/models/*, cmd/server/   │
│ Роль AI: перевод Java-выражений в Go (условно до 70% точности)  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 4. Полуавтоматический перенос бизнес-логики                │
│ - AI строит карту: какие методы Java → какие функции Go         │
│ - для каждого хендлера:                                         │
│     * если логика простая (CRUD, валидация, трансформация)      │
│       → автоматический перевод                                 │
│     * если сложная (транзакции,锁, streams, Optional)          │
│       → пометка как `// TODO manual migration: <объяснение>`   │
│ - инженер в VS Code видит TODO-комментарии с AI-подсказками     │
│ Артефакт: мигрированный код + migration_report.json            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 5. Контрольная точка №2: компиляция и линтеры             │
│ - go build (должен проходить)                                   │
│ - статический анализ (go vet, staticcheck)                      │
│ - проверка, что все импорты/типы определены                     │
│ - генерация недостающих методов (например, для интерфейсов)     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 6. Сравнительное тестирование (ключевая часть!)           │
│ - запускаем Java-сервис (testcontainers или локально)          │
│ - запускаем Go-сервис                                           │
│ - прогоняем тестовый набор (фикстуры запросов):                 │
│     * золотой набор от организаторов                            │
│     * сгенерированные тесты (property-based, diff-тесты)       │
│ - сравниваем: статус-коды, тело ответа (JSON diff), заголовки  │
│ - отчёт о расхождениях с рекомендациями по исправлению          │
│ Артефакт: diff_report.html (зелёное/красное по каждому тесту)  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 7. Итеративная доработка (человек + AI assistant)         │
│ - для каждого расхождения:                                      │
│     * AI предлагает исправление в Go-коде                      │
│     * инженер принимает/правит/отклоняет                        │
│ - после исправлений → возврат к этапу 6                         │
│ - выход: API-совместимость достигнута                           │
└─────────────────────────────────────────────────────────────────┘

агент который получает арзив и прделагает план перехода на go и возвращает zip архив
